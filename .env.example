# 站点根 URL，用于 sitemap、robots、canonical、Open Graph 等。
# 未配置时默认 https://shi-ci.cn；Vercel 未配置时也会使用该默认值。
NEXT_PUBLIC_SITE_URL=https://shi-ci.cn

# .md 仓库地址，用于导航栏 GitHub、详情页「纠错与完善」、页脚纠错/反馈链接（编辑链接指向该仓库下的 author_slug/slug.md）。
# 未配置时默认 https://github.com/daichangya/chinese-poetry-md。
NEXT_PUBLIC_SOURCE_REPO=https://github.com/daichangya/chinese-poetry-md

# 百度统计站点 ID（hm.js? 后面的那串）。未配置或为空时不加载统计脚本；Vercel 未配置时默认不加载。
# NEXT_PUBLIC_BAIDU_ANALYTICS_ID=

# 数据库类型：sqlite（默认）、postgres、turso 或 json。json 时使用 public/data/*.json，无需数据库，适合 Vercel 无库部署。
# DATABASE_TYPE=sqlite
# PostgreSQL 连接串（当 DATABASE_TYPE=postgres 时必填）；SQLite/Turso 时忽略。
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
# Turso（LibSQL）库 URL，仅当 DATABASE_TYPE=turso 时使用；形如 libsql://<db>-<org>.turso.io
# TURSO_DATABASE_URL=libsql://your-db-your-org.turso.io
# Turso 认证 token（DATABASE_TYPE=turso 时使用；本地 file: 可省略）
# TURSO_AUTH_TOKEN=your-turso-auth-token
# JSON 数据源根 URL（仅当 DATABASE_TYPE=json 且从其他域名拉取时设置）；不设则从同源 /data 或本地 public/data 读取。
# NEXT_PUBLIC_DATA_URL=https://your-site.com
# JSON 构建产出目录（build:json），默认 public/data；部署时该目录会随 Next 输出发布为静态资源。
# DATA_JSON_OUTPUT_DIR=public/data
# SQLite 单库数据目录（poetry_index.db 所在目录），可选；未配置时默认为项目根下的 data/。PostgreSQL/Turso/JSON 时忽略。
# DATABASE_DIR=/path/to/project/data

# 分层 SSG：诗详情从热门选集 tag（见 BUILD_SSG_TAG_SLUGS）中收集约 N 首预渲染（默认 5000），作者预渲染前 M 个；其余按需生成。设为 0 则不预渲染。
BUILD_SSG_POEM_LIMIT=5000
BUILD_SSG_AUTHOR_LIMIT=50
# 可选：参与 SSG 的 tag slug，逗号分隔；不设则用代码内默认列表（唐诗三百首、宋词三百首、千家诗、蒙学、古文观止等）
# BUILD_SSG_TAG_SLUGS=tang-shi-san-bai-shou,song-ci-san-bai-shou,qian-jia-shi,meng-xue,gu-wen-guan-zhi

# 分批构建（npm run build:batched，仅静态导出场景）时每批数量，可选；默认动态构建不需要
# BUILD_POEM_LIMIT=2000
# BUILD_AUTHOR_LIMIT=3000

# 作者简介生成脚本（npm run gen_author_bio）读取的 chinese-poetry 根目录，可选；默认 ./chinese-poetry
# CHINESE_POETRY_ROOT=chinese-poetry

# 中间 .md 产出目录，默认 ./chinese-poetry-md。若需推送到独立仓库，可设为已克隆的 chinese-poetry-md 目录（绝对路径或相对项目根）。
# POEMS_DIR=./chinese-poetry-md

# .md 上传目标仓库（push:md 脚本与文档参考；用于校验 remote 与提示）
# CHINESE_POETRY_MD_REPO=https://github.com/daichangya/chinese-poetry-md
